plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
    id("com.facebook.react") // Автоматическое применение через settings.gradle
}

android {
    namespace "com.crossfitcoach"
    compileSdk 34

    defaultConfig {
        applicationId "com.crossfitcoach"
        minSdk 23 // Как указано ранее, для совместимости с 0.74.5
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled true
            proguardFiles getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro"
        }
        debug {
            // Конфигурации для debug-сборки
        }
    }

    buildFeatures {
        viewBinding true
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8"
    }
}

dependencies {
    implementation("com.facebook.react:react-android") // Без версии — плагин сам подтянет 0.74.5 из локального Maven
    implementation("org.jetbrains.kotlin:kotlin-stdlib:1.9.24")
    implementation("androidx.core:core-ktx:1.12.0")
    // Другие зависимости, если есть
}

apply from: file("../../node_modules/@react-native/gradle-plugin/facebook/react-native-screens.preset") // Если используется react-native-screens или подобные
